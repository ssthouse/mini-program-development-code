<view class="lyric-container">
  <view bindtap="closeLyric">
    <view style="transform: translateY(-{{currentIndex*100/(lyric.lyricItems.length+6)}}%)">
<!--      <view wx:if="{{lrc.nolyric}}" class="notext">纯音乐，无歌词</view>-->
<!--      <view wx:if="{{lrc.scroll&&!lrc.nolyric&&!lrc.uncollected}}" class="notext">*歌词不支持滚动*</view>-->
<!--      <view wx:if="{{lrc.uncollected}}" class="notext">暂无歌词</view>-->

      <view wx:for-items="{{lyric.lyricItems}}"
            wx:key="{{index}}"
            class="row {{index===currentIndex ? 'current-row':''}}">
        {{item.content}}
      </view>
<!---->
    </view>
    <view hidden="{{lyric}}" class="loading-panel">歌词加载中...
      <view>
        <text class="cl1"></text>
        <text class="cl2"></text>
        <text class="cl3"></text>
      </view>
    </view>
  </view>
</view>
